# Core API Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# WebSockets & Dateiwächter
watchdog==4.0.0
websockets==12.0

# Pydantic (für Modelle, ist mit FastAPI gekoppelt)
pydantic==2.10.4
typing-extensions>=4.5.0

# Logging
loguru==0.7.2

# Autogen (AG2)
ag2[openai]  # stellt automatisch Autogen + API-Kompatibilität bereit

# ENV-Unterstützung
python-dotenv==1.0.1

